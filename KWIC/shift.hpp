#pragma once
using namespace std;

#include <algorithm>
#include <string>
#include <list>

list<list<string>> shiftTitle(list<string> title);
list<list<string>> shiftTitles(list<list<string>> titles);
